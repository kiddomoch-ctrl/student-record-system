
/fromchartodevicein_room".
     - \param tag of the device
     - \param room_id room id
     - \param category id
     - \param subCategory subcategory id
     - \param device_id device id
     _/
    void OnPostSetRoomDevices(std::string_view tag,
                              const std::string& room_id,
                              const std::string& category,
                              const std::string& subCategory,
                              const std::string& device_id);

    /__
     - @brief Called when a POST request is received to change the room of a device.
     _
     - This function is called when a POST request is received to change the room of a device.
     - The path of the POST request is "/assistant/fromchartodevicein_room".
     - \param tag of the device
     - \param room_id room id
     - \param category id
     - \param subCategory subcategory id
     - \param device_id device id
     _/
    void OnPostSetRoomDevices(std::string_view tag,
                              const std::string& room_id,
                              const std::string& category,
                              const std::string& subCategory,
                              const std::string& device_id);

    /__
     - @brief Called when a POST request is received to change the room of a device.
     _
     - This function is called when a POST request is received to change the room of a device.
     - The path of the POST request is "/assistant/fromchartodevicein_room".
     - \param tag of the device
     - \param room_id room id
     - \param category id
     - \param subCategory subcategory id
     - \param device_id device id
     _/
    void OnPostSetRoomDevices(std::string_view tag,
                              const std::string& room_id,
                              const std::string& category,
                              const std::string& subCategory,
                              const std::string& device_id);

    /__
     - @brief Called when a POST request is received to change the room of a device.
     _
     - This function is called when a POST request is received to change the room of a device.
     - The path of the POST request is "/assistant/fromchartodevicein_room".
     - \param tag of the device
     - \param room_id room id
     - \param category id
     - \param subCategory subcategory id
     - \param device_id device id
     _/
    void OnPostSetRoomDevices(std::string_view tag,
                              const std::string& room_id,
                              const std::string& category,
                              const std::string& subCategory,
                              const std::string& device_id);

    /__
     - @brief Called when a POST request is received to change the room of a device.
     _
     - This function is called when a POST request is received to change the room of a device.
     - The path of the POST request is "/assistant/fromchartodevicein_room".
     - \param tag tag of the device
     - \param room_id room id
     - \param category id
     - \param subCategory subcategory id
     - \param device_id device id
     _/
    void OnPostSetRoomDevices(std::string_view tag,
                              const std::string& room_id,
                              const std::string& category,
                              const std::string& subCategory,
                              const std::string& device_id);

    /__
     - @brief Called when a POST request is received to change the room of a device.
     _
     - This function is called when a POST request is received to change the room of a device.
     - The path of the POST request is "/assistant/fromchartodevicein_room".
     - \param tag of the device
     - \param room_id room id
     - \param category id
     - \param subCategory subcategory id
…
